# 九、前端性能优化（含案例讲解）

[← 返回目录](../README.md)

---

## 1. 首屏优化 checklist

```javascript
// 1. 路由懒加载
const Home = () => import('@/views/Home.vue');

// 2. 图片懒加载 + 合适格式
<img loading="lazy" src="hero.webp" width="800" height="400" />

// 3. 关键 CSS 内联，非关键 JS defer/async
```

**排查白屏**：Performance 看 FCP/LCP、Network 看是否 JS 过大阻塞、是否接口瀑布。

---

## 2. 虚拟列表（el-table / 长列表）

**问题**：10000 行 DOM，滚动卡死。  
**思路**：只渲染可视区域 + 上下缓冲的几十条。

```javascript
// 伪代码
const itemHeight = 40;
const visibleCount = Math.ceil(viewportHeight / itemHeight) + 2;
const start = Math.floor(scrollTop / itemHeight);
const end = start + visibleCount;
const slice = fullList.slice(start, end);
// 外层容器 height = fullList.length * itemHeight（撑开滚动条）
// 内层 translateY(start * itemHeight) 偏移
```

---

## 3. 减少回流

- 读布局属性（offsetHeight）和写 style **分开批量做**  
- 动画优先 `transform`、`opacity`

---

## 4. 打包瘦身

```bash
# 分析包体积
npx vite-bundle-visualizer
# 或 webpack-bundle-analyzer
```

删掉未用依赖、moment 改 dayjs、lodash 改 lodash-es 按需引入。

---

[下一章：安全 →](./10-前端安全.md)
